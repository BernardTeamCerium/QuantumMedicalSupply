# Top Banner (FreeStyle Libre)

The homepage shows a full-width promo banner at the very top. Replace the
placeholder with the real banner image — same filename, no code changes needed.

## Replace it
1. Save the approved Abbott/FreeStyle Libre banner as **`freestyle-libre-3.jpg`**
   (or `.png`) in this folder, overwriting the placeholder.
2. Commit/upload it. Done.

- Recommended width **1240px or larger**, kept fairly short (it spans full width
  and scales down on phones, so avoid tiny legal text if you can).
- The whole banner links to the Typeform eligibility funnel
  (`nocostcgm.typeform.com/...`). Tell me if that should change.
- If you only have a PNG, either save as `.jpg` or tell me and I'll point the
  `<img>` at the `.png`.

> Note: the banner is currently shown only on the homepage, per your choice.
